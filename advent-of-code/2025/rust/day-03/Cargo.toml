[package]
name = "day-03"
edition = "2024"

[features]
default = []
regex = ["dep:regex"]
ffi_pcre2 = []  # For later pcre2 FFI comparison

[dependencies]
regex = { version = "1", optional = true }

[dev-dependencies]
rstest.workspace = true
