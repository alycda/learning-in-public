#arcade #solved

> The pixels in the input image are represented as integers. The algorithm distorts the input image in the following way: Every pixel `x` in the output image has a value equal to the average value of the pixel values from the `3 × 3` square that has its center at `x`, including `x` itself. All the pixels on the border of `x` are then removed.
> 
> Return the blurred image as an integer, with the fractions rounded down.

### PseudoCode
1. There must be at least 3 rows (of 3 columns)
2. Should we calculate the final count of rows and columns beforehand?
	1. Cols = row[0].length - 2?
	2. Rows = rows.length - 2?
3. Let row, col (indices)
4. If a box includes the last index of the last row then the blur is complete
## ecmascript

```typescript

```

## Python

```python

```

## C / C++

```c

```

## Rust

```rust

```
