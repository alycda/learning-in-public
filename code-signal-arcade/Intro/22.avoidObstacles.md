#arcade #unsolved

> You are given an array of integers representing coordinates of obstacles situated on a straight line.
> 
> Assume that you are jumping from the point with coordinate `0` to the right. You are allowed only to make jumps of the same length represented by some integer.
> 
> Find the minimal length of the jump enough to avoid all the obstacles.

### PseudoCode
1. sort the array (numeric)
2. get max length of consecutive numbers
3. modulus?
## ecmascript

```typescript
let solution = (inputArray: number[]): number => {
	const sorted = inputArray.map(Number).sort(compareNumbers)

	return 0
}

function compareNumbers(a, b) {
  return a - b;
}
```

## Python

```python

```

## C / C++

```c

```

## Rust

```rust
// see https://github.com/nolik/CodeSignal/blob/master/src/task/avoid_obstacles.rs
fn solution(inputArray: Vec<i32>) -> {
	for i in 2..std::i32::MAX {
		let if (inputArray.iter().all(|x| (x % i) != 0)) {
			return i
		} else continue
	}
	0
}
```
