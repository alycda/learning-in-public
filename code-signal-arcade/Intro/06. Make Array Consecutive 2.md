#arcade #solved

> Ratiorg got `statues` of _different_ sizes as a present from CodeMaster for his birthday, each statue having an non-negative integer size. Since he likes to make things perfect, he wants to arrange them from smallest to largest so that each statue will be bigger than the previous one exactly by `1`. He may need some additional statues to be able to accomplish that. Help him figure out the minimum number of additional statues needed.

### PseudoCode
1. 
## ecmascript

```typescript
// return Math.max(...sequence)-Math.min(...sequence)+1-sequence.length

function solution(statues) {
	let missing = 0;
	statues.sort((a, b) => (a - b))
	
	for(let i = 0; i < statues.length - 1; i++) {
		const current = statues[i],
		next = statues[i + 1]
		
		if (next != current + 1) {
			missing += next - current -1
		}
	}
	
	return missing
}
```

## [[Python]]

```python
def solution(statues):
  return max(statues) - min(statues) - len(statues) + 1
```

## [[C]] / [[C++]]

```c

```

## [[Personal/Computer Science/Rust]]

https://app.codesignal.com/arcade/intro/level-2/bq2XnSr5kbHqpHGJC/solutions?solutionId=yGCPSNTaHvxaHhcdh

```rust
// fn solution(mut statues: Vec<i32>) -> i32 {
//   statues.sort();
//   statues.windows(2).map(|pair| pair[1] - pair[0] - 1).sum()
// }

fn solution(statues: Vec<i32>) -> i32 {
	let length = statues.len() as i32;	
	let mut max: i32 = statues[0];
	
	if length < 2 { return max - 1 };
	
	let mut min: i32 = statues[0];	
	let mut idx = 0;
	
	loop {
		idx += 1;
		
		if(idx == length) {		
			break
		}
		
		max = std::cmp::max(max, statues[idx]);		
		min = std::cmp::min(min, statues[idx]);
	
	}
	
	// println!("Max: {}, Min: {}", max, min);
	max - min - length + 1
}
```
