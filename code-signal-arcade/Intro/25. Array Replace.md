#arcade #solved

> Given an array of integers, replace all the occurrences of `elemToReplace` with `substitutionElem`.

### PseudoCode
1. 
## ecmascript

```typescript
let solution(inputArray: number[], elemToReplace: number, substitutionElem: number): number[] => inputArray.map(x => x == elemToReplace ? substitutionElem : x)
```

## Python

```python
def solution():
  return
```

## C / C++

```c

```

## Rust

```rust
fn solution (input: Vec<i32>, replace: i32, substitute: i32) -> Vec<i32> {
  input.iter_mut().map(|x| if (x == replace) { substitute } else { x }).collect()
}
```

---

```dataview
table without id file.inlinks where file.name = this.file.name
```